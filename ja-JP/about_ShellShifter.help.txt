TOPIC
    各種シェル(主にbash)を起動する為の機能を提供します

SHORT DESCRIPTION
    各種シェル(主にbash)を起動する為の機能を提供します

LONG DESCRIPTION
    特別な設定をしなくても自動的にインストール済みのアプリケーションを判別し、各種シェル(主にbash)を起動することが出来ます。

EXAMPLES
    msys -c 'echo aaa'
    echo 'echo aaa' | msys

KEYWORDS
    msys cygwin gitbash bash

SEE ALSO
    --
